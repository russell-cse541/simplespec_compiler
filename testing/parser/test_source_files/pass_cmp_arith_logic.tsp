// This is a comment

namespace  Main;

using BigCode;
using BadCode;
using Hope;
//one more comment 
function checkit(u: User): Bool {
    let a:Bool = -5 < 9i + 7n;
    let b:Bool = true && false && !(!(true || false) && !(t<s));
    let c:Int = 5 + 4 - 7 + 11 - 15;
    let d:Int = 4 + (4 - 2); //how about another comment
    let e:Bool = t > w;
    let f:Bool = a ==> b;
}
